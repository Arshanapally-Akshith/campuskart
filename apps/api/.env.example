# Production values for every var below: see docs/DEPLOYMENT.md.
NODE_ENV=development
PORT=4000
LOG_LEVEL=info

# Comma-separated list of allowed browser origins.
CORS_ORIGIN=http://localhost:5173

# Refresh-token cookie's SameSite attribute. 'lax' works for local dev
# (web and api are same-site there). A split-domain prod deploy (Vercel web,
# Render/Fly api) is cross-site, so a cross-site fetch never attaches a Lax
# cookie — set COOKIE_SAME_SITE=none in production instead.
COOKIE_SAME_SITE=lax

# Matches the credentials in the root .env.example used by docker-compose.
# Production: an Atlas SRV URI (mongodb+srv://user:pass@cluster.mongodb.net/campuskart)
# and an Upstash rediss:// URI (TLS — ioredis picks up TLS automatically
# from the "rediss" scheme, no extra config needed).
MONGO_URI=mongodb://campuskart:campuskart_dev_password@localhost:27017/campuskart?authSource=admin
REDIS_URL=redis://localhost:6379

# Symmetric secret for signing access JWTs. Generate a real one for anything
# beyond local dev, e.g.: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=dev_only_change_me_dev_only_change_me

# Optional. From your Cloudinary dashboard (console.cloudinary.com) — a free
# account is enough for dev. Leave all three unset/blank to run without the
# image pipeline: the API/worker still start normally, and anything that
# needs Cloudinary (/api/uploads/sign, thumbnail generation, orphan cleanup)
# returns a 503 SERVICE_UNAVAILABLE instead of a broken upload — see
# docs/PHASE3_NOTES.md.
CLOUDINARY_CLOUD_NAME=dev_only_change_me
CLOUDINARY_API_KEY=dev_only_change_me
CLOUDINARY_API_SECRET=dev_only_change_me
