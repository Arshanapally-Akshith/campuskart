/** Populated by the mocked mailer in tests/setup.ts; tests read the OTP a
 * "sent" email would have contained without ever exposing it via the API. */
export const sentOtps = new Map<string, string>();
